local a=require(settings.get("ghu.base").."core/apis/ghu")if a.s.minified.get()then print("Updating repos (min)...")else print("Updating repos (full)...")end;local b,c=a.updateRepo("core")for d,e in pairs(a.s.extraRepos.get())do local f,g=a.updateRepo(e)b=b+f;c=c+g end;print(string.format("Updated %s file%s from %s repo%s",b,b~=1 and"s"or"",c,c~=1 and"s"or""))if b>1 then local h=a.s.autoUpdate.get()a.s.autoUpdate.set(false)shell.run(settings.get("ghu.base").."core/programs/ghureload")a.s.autoUpdate.set(h)end
